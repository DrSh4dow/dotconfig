#!/bin/sh
set -eu

if command -v dunstctl >/dev/null 2>&1; then
    dunstctl set-paused false >/dev/null 2>&1 || true
fi

if command -v waybar >/dev/null 2>&1 && ! pgrep -x waybar >/dev/null 2>&1; then
    waybar >/tmp/waybar-gamemode.log 2>&1 &
fi
